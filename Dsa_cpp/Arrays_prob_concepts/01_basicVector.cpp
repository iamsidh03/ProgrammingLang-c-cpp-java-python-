// Online C++ compiler to run C++ program online
#include <iostream>
using namespace std;
#include <vector>

int main() {
    
    vector<int> arr;
    // int ans=(sizeof(arr)/sizeof(int));
    // cout<<ans<<endl;
    //cout<<arr.size()<<endl;
    //size gives you number of element in you array
   // cout<<arr.capacity()<<endl;
   // capacity gives you how many element you can store in your array
    /*
    //insert
    arr.push_back(5);
    arr.push_back(55);
    arr.push_back(50);
    //print
    for(int i=0;i<arr.size();i++){
        cout<< arr[i] << " ";
    }
    cout<<endl;
    //remove / delete
    arr.pop_back();
    cout<<"after pop an element"<<endl;
    for(int i=0;i<arr.size();i++){
        cout<< arr[i] << " ";
    }
    cout<<endl;
    */
    /*
    
    //creating another array (brr) of size 10
    vector<int> brr(10);
    cout<< "size of b " <<brr.size()<<endl;
    cout<<"capacity of b "<<brr.capacity<<endl;
    
    */
    /*
    //another array size will define by user and put default value as 100
    int n;
    cout << " Enter the size of array";
    cin >> n;
    vector<int> crr(n,100);
    //printing brr
    for(int i=0;i<crr.size();i++){
        cout<< crr[i] << " ";
    }
    cout<<endl;
    cout<< "vector crr is empty or not "<<crr.empty()<<endl;
    */
    
    
    return 0;
}